server.port: 9998

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    reactor.netty.http.client: DEBUG

spring:
  application:
    name: gateway-admin
  config:
    import:
      - aws-secretsmanager:/secret/algashop/gateway-admin/redis-password?prefix=gateway-admin.secrets.
      - aws-parameterstore:/config/algashop/gateway-admin?prefix=gateway-admin.params
      - aws-parameterstore:/config/algashop/shared?prefix=shared.params
  data:
    redis:
      host: ${gateway-admin.params.redis.host}
      port: ${gateway-admin.params.redis.port}
      password: ${gateway-admin.secrets.redis-password}
      database: ${gateway-admin.params.redis.database}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${shared.params.auth-server-url}
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      region:
        static: us-east-1
      endpoint: http://algashop-localstack:4566
    gateway:
      server:
        webflux:
          route-refresh-listener:
            enabled: false
          default-filters:
            - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST
            - name: Retry
              args:
                retries: 3
                statuses: BAD_GATEWAY, INTERNAL_SERVER_ERROR
                methods: GET,PUT
                exceptions: java.io.IOException, java.util.concurrent.TimeoutException
                backoff:
                  firstBackoff: 10ms
                  maxBackoff: 20ms
          httpclient:
            connect-timeout: ${gateway-admin.params.httpclient.connect-timeout}
            response-timeout: ${gateway-admin.params.httpclient.response-timeout}
          globalcors:
            add-to-simple-url-handler-mapping: true
            cors-configurations:
              '[/**]':
                allowedOrigins: ${gateway-admin.params.cors.allowed-origin}
                allowedHeaders: '*'
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                  - OPTIONS
          routes:
            - id: orders-route
              uri: lb://ordering
              predicates:
                - Path=/api/v1/orders/**
                - Method=GET,POST,PUT,DELETE,OPTIONS
              filters:
                - name: CircuitBreaker
                  args:
                    name: ordering-route-cb
                    statusCodes:
                      - 502
                      - 504
                      - 500
            - id: customers-route
              uri: lb://ordering
              predicates:
                - Path=/api/v1/customers/**
                - Method=GET,OPTIONS
            - id: product-catalog-list-route
              uri: lb://product-catalog
              predicates:
                - Path=/api/v1/products
                - Method=GET
              filters:
                - RemoveJsonAttributesResponseBody=shortDescription,mainImage,true
            - id: product-catalog-route
              uri: lb://product-catalog
              predicates:
                - Path=/api/v1/products,/api/v1/products/**,/api/v1/categories/**,/api/v1/upload-requests/**
                - Method=GET,POST,PUT,DELETE,OPTIONS
              filters:
                #- LocalResponseCache=5m,30MB
                - name: RequestRateLimiter
                  args:
                    redis-rate-limiter:
                      replenishRate: 50
                      burstCapacity: 200
                      requestedTokens: 1
                    key-resolver: "#{@rateLimitKeyResolver}"
            - id: user-management-route
              uri: lb://authorization-server
              predicates:
                - Path=/api/v1/users/**
                - Method=GET,POST,PUT,DELETE,OPTIONS

eureka:
  client:
    serviceUrl:
      defaultZone: ${shared.params.service-registry-url}/eureka
    registerWithEureka: false
    fetchRegistry: true
  instance:
    instanceId: ${random.uuid}
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health, gateway, circuitbreakers
  endpoint:
    gateway:
      enabled: true

resilience4j.circuitbreaker:
  instances:
    ordering-route-cb:
      slidingWindowSize: 8 # total calls to analyze
      failureRateThreshold: 50 # 50%
      minimumNumberOfCalls: 5 # minimum number of failures to calculate if its failing
      permittedNumberOfCallsInHalfOpenState: 5
      slidingWindowType: COUNT_BASED # / TIME_BASED
      waitDurationInOpenState: 10s
      maxWaitDurationInHalfOpenState: 30s
      registerHealthIndicator: true # enable circuitbreakers health check